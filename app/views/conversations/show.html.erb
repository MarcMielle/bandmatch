<div>
  <% @messages.each do |message| %>
    <p><%= message.content %></p>
  <% end %>
</div>
<%= simple_form_for [ @conversation, @message ] do |f| %>
  <%= f.input :content, label: false, placeholder: "Message ##{@conversation.id}" %>
  <%= f.submit %>
<% end %>
